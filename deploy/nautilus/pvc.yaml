# RWX PVC for /files (rook-cephfs common on NRP). kubectl apply -f deploy/nautilus/pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unc-files
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: rook-cephfs
  resources:
    requests:
      storage: 2Ti
